import { ObjectId } from 'mongodb'
import { UpdateTweetReqBody } from '~/models/requests/tweets.request'

class UploadServices {
  async uploadImgToClaud() {}
}
const uploadServices = new UploadServices()
export default uploadServices
